架构设计文档

# 云平台自动化维护中心 V1——系统详细架构设计

**文档版本：** V1.0
**对应需求：**《云平台自动化维护中心 V1 需求规格说明书》
**目标环境：** CentOS 9
**部署方式：** Docker
**执行底座：** SaltStack Master / Minion / salt-api
**数据库：** SQLite
**目标规模：** ≤ 30 台受管节点

------

# 1. 设计目标

Automation Center V1 目标是建设一个轻量的云平台维护任务管理系统，用于将研发侧已经验证完成的维护包，下发到指定云平台节点执行。

系统自身不理解 Nova、Neutron、OVS、Ceph 等具体业务逻辑。

职责边界为：

```text
维护包
负责：
“怎么修”

Automation Center
负责：
“在哪些节点执行”
“以什么顺序执行”
“当前执行到哪里”
“执行结果是什么”

SaltStack
负责：
“把任务可靠送到节点”
“执行系统命令”
“传输文件”

Salt Minion
负责：
“在目标节点实际执行”
```

V1 设计原则：

1. 单节点；
2. 少依赖；
3. 不引入 Redis/RabbitMQ/Kafka；
4. 不设计复杂 Workflow Engine；
5. 节点级任务串行；
6. 节点之间并发；
7. 所有运行状态落库；
8. Automation Center 重启后可恢复运行中的任务；
9. 实时日志可查看；
10. 底层 Salt 能力与上层业务模型解耦。

------

# 2. 总体架构

```text
                         用户浏览器
                             │
                             │ HTTP/HTTPS
                             ▼
                 ┌──────────────────────┐
                 │  Automation Center   │
                 │                      │
                 │  Web UI              │
                 │  REST API            │
                 │  Package Service     │
                 │  Node Service        │
                 │  Task Service        │
                 │  Scheduler           │
                 │  Log Service         │
                 │  Salt Adapter        │
                 │  SQLite              │
                 └──────────┬───────────┘
                            │
                            │ salt-api
                            ▼
                     ┌────────────┐
                     │  salt-api  │
                     └──────┬─────┘
                            │
                            ▼
                    ┌───────────────┐
                    │  salt-master  │
                    │               │
                    │  Fileserver   │
                    │  Job Cache    │
                    └───────┬───────┘
                            │
                     TCP 4505/4506
                            │
          ┌─────────────────┼─────────────────┐
          ▼                 ▼                 ▼
    salt-minion        salt-minion       salt-minion
     compute01          network01           ceph01
          │                 │                 │
          ▼                 ▼                 ▼
       Shell             Python             Shell
```

------

# 3. 部署架构

## 3.1 管理节点

V1 可以在一台 CentOS 9 管理节点部署：

```text
CentOS 9
│
├── Docker
│   └── automation-center
│
├── salt-master
│
└── salt-api
```

Salt Master 与 salt-api 可继续作为宿主机 systemd 服务运行。

Automation Center 通过 HTTP/HTTPS 调用 salt-api。

------

# 3.2 Docker 持久化目录

建议宿主机：

```text
/var/lib/automation-center/
├── data/
│   └── automation.db
│
├── packages/
│   └── <package-id>/
│       └── payload.tar.gz
│
├── logs/
│   └── <task-id>/
│
└── temp/
```

容器通过 bind mount 挂载：

```text
/var/lib/automation-center
```

数据库、维护包和日志不得只存储于 Docker 可写层。

------

# 3.3 Salt Fileserver 集成

推荐让 Salt Master Fileserver 可以只读访问：

```text
/var/lib/automation-center/packages/
```

形成：

```text
Automation Center
      │
      │ 写入
      ▼
/var/lib/automation-center/packages/
      │
      │ Salt Master 只读
      ▼
Salt Fileserver
      │
      ▼
salt-minion
```

这样不需要：

- MinIO；
- HTTP 文件下载服务；
- NFS；
- 上传包二次复制到 `/srv/salt`。

Package Storage 目录需要同时满足：

```text
Automation Center → 可读写
salt-master       → 可读
```

------

# 4. Automation Center 内部模块

推荐拆分为以下逻辑模块：

```text
Automation Center
├── Auth Service
├── Dashboard Service
├── Node Service
├── Package Service
├── Task Service
├── Scheduler
├── Execution Engine
├── Salt Adapter
├── Log Service
├── Settings Service
├── Audit Service
└── Persistence Layer
```

V1 不要求这些模块拆成独立微服务。

建议全部运行在一个后端进程中。

------

# 5. Web 层

Web 主要负责：

```text
Login
Dashboard
Node Center
Pending Nodes
Package Center
Task Center
System Settings
```

Web 不直接访问 Salt API。

必须采用：

```text
Browser
   ↓
Automation Center Backend
   ↓
salt-api
```

禁止：

```text
Browser
   ↓
salt-api
```

从而避免 Salt credential 暴露到浏览器。

------

# 6. 后端 API 层

V1 API 只供自身 Web 使用。

建议资源模型：

```text
/api/v1/auth
/api/v1/dashboard
/api/v1/nodes
/api/v1/packages
/api/v1/tasks
/api/v1/settings
/api/v1/audit
```

API 虽然不正式对外，但应保持 REST 风格，避免前端直接依赖数据库或 Salt 内部结构。

------

# 7. Salt Adapter

Salt Adapter 是 Automation Center 与 SaltStack 之间唯一接口层。

职责：

```text
Salt Adapter
├── Authentication
├── Node Ping
├── Key Management
├── Remote Command
├── File Transfer
├── Async Job
├── Job Status
├── Job Result
└── Remote Log Read
```

其他业务模块不得直接拼装 Salt API 请求。

这样未来底层 Salt API 调用方式变化时，只修改 Salt Adapter。

------

# 8. Node Service

Node Service 管理：

```text
Node
├── Salt Minion ID
├── Hostname
├── Management IP
├── Online Status
├── Enabled Status
├── Roles
└── Last Check Time
```

------

# 9. 新节点接入

流程：

```text
salt-minion
    ↓
salt-master
    ↓
Unaccepted Key
    ↓
Node Service 定期获取
    ↓
Pending Nodes 页面
    │
    ├── Accept
    └── Reject
```

Accept 后：

```text
Salt Key Accepted
      ↓
test.ping
      ↓
采集基础信息
      ↓
执行角色检测
      ↓
创建 Node
```

------

# 10. 节点状态检测

后台 Node Monitor 定期执行：

```text
test.ping
```

建议默认周期：

```text
60 秒
```

但实际值由：

```text
node_status_check_interval
```

配置。

状态模型：

```text
最后探测成功
→ Online

最后探测失败
→ Offline
```

页面允许立即手工刷新。

------

# 11. Role Detection

V1 根据节点实际运行服务/进程自动判断角色。

建议定义配置规则：

```text
role_detection_rules
```

概念模型：

```text
RoleRule
├── role
├── matcher_type
└── pattern
```

例如：

```text
compute
  process/service: nova-compute

ceph
  process/service: ceph-osd

network
  process/service: ovn-controller
  process/service: ovs-vswitchd

controller
  process/service: nova-api
```

一台节点可以同时匹配多个 Role。

------

# 12. 自动 Role 与人工 Role

为了避免定时扫描覆盖人工配置，建议内部区分：

```text
Detected Roles
Manual Override
Effective Roles
```

规则：

```text
如果没有人工修改：
Effective Roles = Detected Roles

如果用户人工修改：
Effective Roles = Manual Roles
```

用户可执行“恢复自动识别”，重新使用 Detected Roles。

------

# 13. Package Service

Package Service 负责：

```text
上传
解包
SHA256 校验
读取 Manifest
保存 Metadata
保存 payload
更新 Revision
删除 Package
```

不负责：

```text
脚本语义校验
业务逻辑校验
组件兼容性验证
维护动作验证
```

------

# 14. Package 上传流程

```text
Web 上传 Bundle
      ↓
流式写入 temp
      ↓
解压外层 Bundle
      ↓
读取 payload.tar.gz
      ↓
读取 payload.sha256
      ↓
SHA256(payload.tar.gz)
      ↓
       比对
    ┌────┴────┐
   不一致     一致
    │          │
 Reject     临时解析 payload
               ↓
          读取 manifest.yaml
               ↓
          保存 Package Metadata
               ↓
          保存 payload.tar.gz
               ↓
           Revision = v1
               ↓
           清理 temp
```

10GB 上传必须采用流式写盘，不允许整体读入内存。

------

# 15. Package 更新流程

```text
用户选择 Update
      ↓
检查 Package
      ↓
是否存在 Running/Waiting Task？
      │
   ┌──┴──┐
   │     │
  Yes    No
   │     │
 Reject  上传新 Bundle
          ↓
       SHA256 校验
          ↓
       读取 Manifest
          ↓
       覆盖 payload
          ↓
       Revision + 1
```

更新时 Manifest 中：

```text
name
```

必须与待更新 Package 的逻辑名称一致。

------

# 16. Task Service

Task Service 负责：

```text
创建 Task
生成 Task Name
创建 TaskNode
保存 Node 快照
任务确认
复制 Task
Retry
状态聚合
历史查询
```

Task Service 不直接执行 Salt 命令。

实际执行交给 Scheduler + Execution Engine。

------

# 17. Task 创建

创建过程：

```text
选择 Package
      ↓
选择 Role / Node
      ↓
过滤 Disabled
      ↓
检测 Offline
      ↓
检测 Target Role mismatch
      ↓
填写 Remark
      ↓
确认页
      ↓
幂等检查
      ↓
创建 Task
      ↓
创建 TaskNode
```

Task 创建后立刻进入调度流程。

------

# 18. Task 快照

为了避免后续 Node/Package 被删除或修改导致历史信息变化，创建 Task 时必须保存：

```text
Package Name Snapshot
Package Revision Snapshot
Package Description Snapshot

Node Hostname Snapshot
Node Management IP Snapshot
Node Roles Snapshot
```

历史任务页面优先读取 Snapshot。

------

# 19. 节点级调度

V1 调度核心原则：

> 同一节点同时只能运行一个 TaskNode。

不同节点可以并发。

例如：

```text
Task-A
├── compute01 → Running
├── compute02 → Waiting
└── compute03 → Running
```

------

# 20. FIFO 队列

每个 Node 维护逻辑 FIFO：

```text
compute01
├── Task-A Running
├── Task-B Waiting
└── Task-C Waiting
```

Task-A 完成：

```text
Task-B
→ Running
```

排序依据建议：

```text
TaskNode.created_at
+
TaskNode.id
```

无需新增 RabbitMQ/Redis。

SQLite 本身即为队列状态事实来源。

------

# 21. Node Execution Lock

建议逻辑结构：

```text
NodeExecutionLock
├── node_id
├── task_node_id
└── acquired_at
```

同一个：

```text
node_id
```

最多存在一个有效执行锁。

调度器在启动 TaskNode 前：

1. 尝试获取节点锁；
2. 成功则 Running；
3. 失败则保持 Waiting。

TaskNode 结束后立即释放。

------

# 22. Waiting 取消

Waiting TaskNode 尚未真正下发。

用户可以取消：

```text
Waiting
→ Cancelled
```

如果一个 Task 中：

```text
部分节点 Running
部分节点 Waiting
```

仅允许取消 Waiting 的 NodeTask。

Running 节点不允许停止。

------

# 23. Execution Engine

Execution Engine 负责一个 NodeTask 的完整生命周期：

```text
Acquire Node Lock
      ↓
Prepare Workspace
      ↓
Transfer Package
      ↓
Extract Package
      ↓
Execute Steps
      ↓
Collect Result
      ↓
Cleanup / Retain Workspace
      ↓
Release Node Lock
```

------

# 24. 节点工作目录

目标节点：

```text
/var/lib/automation-center/tasks/<task-id>/
├── package.tar.gz
├── work/
│   ├── manifest.yaml
│   ├── scripts/
│   └── files/
└── runtime/
    └── attempt-<n>/
        ├── step-01.stdout
        ├── step-01.stderr
        ├── step-01.exit
        └── ...
```

Retry 时仍使用同一 Task ID，但创建新的 Attempt 运行目录。

------

# 25. Package 下发

推荐：

```text
Salt Fileserver
       ↓
salt-minion
       ↓
/var/lib/automation-center/tasks/<task-id>/package.tar.gz
```

下发失败：

```text
TaskNode = Failed
failure_reason = PackageTransferFailed
```

------

# 26. Step Executor

统一抽象：

```text
Executor
├── ShellExecutor
├── PythonExecutor
└── AnsibleExecutor    # Future
```

V1：

```text
ShellExecutor
PythonExecutor
```

------

# 27. Shell Executor

语义：

```text
type: shell
```

执行：

```text
/bin/bash <script>
```

工作目录：

```text
work/
```

------

# 28. Python Executor

语义：

```text
type: python
```

执行：

```text
python3 <script>
```

工作目录：

```text
work/
```

V1 默认要求目标节点具有可用：

```text
python3
```

如果不存在：

```text
Step Failed
failure_reason = InterpreterNotFound
```

------

# 29. Step Timeout

实际 timeout：

```text
manifest.timeout
```

如果不存在：

```text
System Settings.default_step_timeout
```

如果仍未设置：

```text
1800 秒
```

超时：

```text
Step.status = Failed
failure_reason = Timeout
```

------

# 30. failure_action

推荐内部增加：

```text
ignored = true/false
```

避免混淆“执行失败”和“流程是否继续”。

## stop

```text
Step Failed
ignored = false
      ↓
后续 Step = Skipped
      ↓
NodeTask Failed
```

## ignore

```text
Step Failed
ignored = true
      ↓
继续下一 Step
```

### Node 最终状态建议

V1 建议定义：

```text
如果仅存在 failure_action=ignore 的失败：
NodeTask 最终仍可以 Success
但 has_warning = true

如果存在非忽略失败：
NodeTask = Failed
```

这样 `ignore` 才真正具有“该错误可以忽略”的语义。

Task 页面可以显示：

```text
Success ⚠
```

但不新增新的 Task 顶层状态。

------

# 31. 实时日志设计

Salt 普通远程执行结果通常更适合“任务完成后取结果”。

为了满足 V1：

```text
执行过程中实时查看 stdout/stderr
```

建议采用：

> 节点本地文件持续写日志 + Automation Center 增量读取 + 浏览器流式展示。

------

# 32. 实时日志流程

```text
Step Start
   ↓
目标节点启动脚本
   │
   ├── stdout → step.stdout
   └── stderr → step.stderr
             │
             ▼
Automation Center
每 1~2 秒读取新增内容
             │
             ▼
       Log Service
             │
             ▼
     Browser Stream
```

因为 V1 同一时间只要求查看一个节点实时日志，所以这种方式不会造成较大 Salt 调用压力。

------

# 33. 浏览器日志流

Backend 与 Browser 之间可以采用：

```text
SSE
或
WebSocket
```

详细实现阶段建议优先 SSE，因为当前主要是：

```text
Server → Browser
```

单向日志流。

需求层不强制具体协议。

------

# 34. 中央日志持久化

Automation Center 获取的 stdout/stderr 同时持久化：

```text
/var/lib/automation-center/logs/
└── <task-id>/
    └── <node-id>/
        └── attempt-1/
            ├── step-01.stdout
            └── step-01.stderr
```

SQLite 只保存：

```text
log_path
log_size
status
exit_code
```

不建议将大量 stdout/stderr 全部塞入 SQLite BLOB/TEXT 字段。

------

# 35. 日志保留

默认：

```text
execution_log_retention_days = 7
```

清理器每日执行：

```text
扫描 finished_at
      ↓
超过 retention
      ↓
删除 stdout/stderr 文件
      ↓
数据库保留 Task/Step 元数据
```

------

# 36. 失败节点工作目录清理

目标节点失败目录：

```text
固定保存 7 天
```

可由 Automation Center 定期通过 Salt 执行清理命令。

成功任务：

```text
任务完成后立即删除工作目录
```

------

# 37. Retry

Retry 作用于：

```text
TaskNode
```

而不是整个 Task。

执行前检查：

```text
Package 是否存在
Revision 是否相同
Node 是否 Enabled
Node 是否可参与任务
```

通过后：

```text
attempt_no + 1
```

并重新：

```text
Step1
→ Step2
→ ...
```

------

# 38. Retry 数据关系

```text
Task
└── TaskNode
    ├── Attempt 1
    │   ├── Step 1
    │   ├── Step 2
    │   └── Step 3
    │
    └── Attempt 2
        ├── Step 1
        ├── Step 2
        └── Step 3
```

NodeTask 当前状态取：

```text
最后一个 Attempt 的最终结果
```

历史 Attempt 不能覆盖。

------

# 39. Task 状态聚合

推荐算法：

## Waiting

所有未取消节点均处于 Waiting。

## Running

至少一个节点：

```text
Running
或
Waiting behind another task
```

且 Task 尚未全部终态。

## Success

所有有效目标节点最终：

```text
Success
```

包括只产生 ignored warning 的节点。

## Partial Success

至少：

```text
1 Success
+
1 Failed/Offline
```

## Failed

所有有效目标节点最终失败。

## Cancelled

如果全部尚未执行的节点均被取消，且不存在成功/失败执行结果，可记 Cancelled。

------

# 40. SQLite 设计原则

V1 使用 SQLite 时建议：

1. 开启 WAL；
2. 所有任务状态更新采用短事务；
3. 避免日志正文写数据库；
4. 后端 V1 使用单主进程；
5. Scheduler 与 API 共用同一数据库；
6. 使用事务保护 Node Lock；
7. 避免多个 Docker 实例同时访问同一个 SQLite 文件。

------

# 41. 主要表关系

```text
Node
  │
  └──< NodeRole >── Role

Package
  │
  └──< PackageStep

Task
  │
  └──< TaskNode
          │
          └──< TaskAttempt
                    │
                    └──< TaskStepResult
```

另外：

```text
SystemSetting
AuditLog
```

独立存在。

------

# 42. 推荐核心表

## nodes

```text
id
salt_minion_id
hostname
management_ip
online_status
enabled_status
last_check_time
created_at
updated_at
```

## roles

```text
id
name
```

## node_roles

```text
node_id
role_id
source
```

## packages

```text
id
name
revision
description
component
bug_id
target_roles_json
applicable_versions_json
payload_path
sha256
created_at
updated_at
```

## package_steps

```text
id
package_id
sequence
name
executor_type
script_path
timeout
failure_action
```

## tasks

```text
id
name
package_id
package_name_snapshot
package_revision_snapshot
status
remark
created_at
started_at
finished_at
idempotency_key
```

## task_nodes

```text
id
task_id
node_id
hostname_snapshot
management_ip_snapshot
roles_snapshot
status
failure_reason
has_warning
created_at
started_at
finished_at
```

## task_attempts

```text
id
task_node_id
attempt_no
status
created_at
started_at
finished_at
```

## task_step_results

```text
id
attempt_id
sequence
step_name
executor_type
status
ignored
salt_jid
exit_code
failure_reason
stdout_path
stderr_path
started_at
finished_at
```

## audit_logs

略。

## system_settings

略。

------

# 43. Salt JID 关联

为了支持服务重启恢复，每一个正在运行的远程 Step 应保存：

```text
salt_jid
```

关联关系：

```text
TaskStepResult
      │
      └── salt_jid
              │
              ▼
          Salt Job
```

Automation Center 不应只依赖内存中的 Future/Thread 判断任务状态。

------

# 44. Automation Center 重启恢复

启动后执行 Recovery：

```text
启动
 ↓
读取 DB
 ↓
查询所有：
TaskNode = Running
Step = Running
 ↓
读取 salt_jid
 ↓
查询 Salt Job
```

结果：

```text
Job 仍在运行
→ 恢复日志轮询和状态跟踪

Job 已完成
→ 获取最终结果并更新 DB

Job 不存在，但节点存在 exit marker
→ 从工作目录恢复结果

Job 与现场状态均不存在
→ Step Failed
   failure_reason = ExecutionStateLost
```

然后：

```text
重建 Node Locks
重新扫描 Waiting Queue
```

------

# 45. Scheduler

Scheduler 建议是后台周期循环：

```text
每 1 秒
    ↓
扫描 Waiting TaskNode
    ↓
按 Node 分组
    ↓
选每个 Node FIFO 第一条
    ↓
尝试 Node Lock
    ↓
开始执行
```

30 节点规模无需复杂分布式 Scheduler。

------

# 46. 防重复执行

创建 Task 时由前端：

```text
点击后禁用按钮
```

Backend 同时生成/验证：

```text
idempotency_key
```

相同创建请求短时间重复提交时不得创建多个 Task。

------

# 47. Authentication

Automation Center 使用自身固定账号。

推荐：

```text
username
password_hash
```

密码只保存 Hash。

登录后使用：

```text
Session Cookie
或
短期 Token
```

V1 不与 Linux `/etc/passwd` 或 Salt PAM 关联。

------

# 48. Salt Credential

System Settings 中：

```text
salt_api_username
salt_api_credential
```

Credential 必须加密保存。

建议加密密钥不存入 SQLite，而通过：

```text
Docker Environment
或
挂载 Secret File
```

提供。

Web 返回：

```text
********
```

不得回显明文。

------

# 49. Audit

重要写操作均写入 AuditLog。

至少包括：

```text
Login
Accept Node
Reject Node
Edit Roles
Enable/Disable Node
Delete Node
Upload Package
Update Package
Delete Package
Create Task
Retry
Cancel Waiting
Update Settings
```

共享账号情况下仍记录：

```text
source_ip
operation_time
```

------

# 50. Package 解压安全

虽然业务层只要求 SHA256 校验，但解压过程必须具备基础系统安全保护：

```text
禁止 ../
禁止绝对路径
禁止写出 temp/work 根目录
限制解压后总大小
限制文件数量
```

这属于平台安全，不属于维护包业务校验。

------

# 51. 10GB 上传设计

上传流程必须：

```text
HTTP Stream
   ↓
临时文件
```

不能：

```text
10GB
↓
一次性加载到 RAM
```

同时需要检查：

```text
磁盘剩余空间
max_upload_size
```

不足时提前拒绝。

------

# 52. Salt 不可用

创建任务前执行 Salt 健康检查。

如果 Salt API 不可达：

```text
Task 不创建
HTTP 返回明确错误
```

已经运行中的任务如果短暂无法查询 Salt：

```text
保持 Running
记录 communication error
继续重试查询
```

不立即重复下发任务。

------

# 53. Package 操作锁

Package 若关联：

```text
Running Task
Waiting Task
```

则：

```text
Update = Forbidden
Delete = Forbidden
```

避免正在执行的文件被替换。

------

# 54. API 规划

建议核心 API：

```text
POST   /api/v1/auth/login
POST   /api/v1/auth/logout

GET    /api/v1/dashboard

GET    /api/v1/nodes
GET    /api/v1/nodes/pending
POST   /api/v1/nodes/pending/{id}/accept
POST   /api/v1/nodes/pending/{id}/reject
PATCH  /api/v1/nodes/{id}
DELETE /api/v1/nodes/{id}
POST   /api/v1/nodes/refresh

GET    /api/v1/packages
POST   /api/v1/packages
GET    /api/v1/packages/{id}
POST   /api/v1/packages/{id}/update
DELETE /api/v1/packages/{id}

GET    /api/v1/tasks
POST   /api/v1/tasks
GET    /api/v1/tasks/{id}
POST   /api/v1/tasks/{id}/copy
POST   /api/v1/tasks/{id}/nodes/{node_id}/retry
POST   /api/v1/tasks/{id}/nodes/{node_id}/cancel

GET    /api/v1/tasks/{id}/nodes/{node_id}
GET    /api/v1/tasks/{id}/nodes/{node_id}/logs

GET    /api/v1/settings
PATCH  /api/v1/settings
```

实时日志可以额外提供：

```text
GET /api/v1/tasks/{task}/nodes/{node}/log-stream
```

------

# 55. 页面规划

```text
/login

/dashboard

/nodes
/nodes/pending

/packages
/packages/:id
/packages/upload
/packages/:id/update

/tasks
/tasks/create
/tasks/:id
/tasks/:id/nodes/:nodeId

/settings
```

------

# 56. Dashboard

Dashboard 后端只聚合当前数据库数据，不直接实时查询所有节点。

节点状态来自：

```text
Node Monitor 最近一次探测结果
```

减少首页访问对 Salt 的压力。

------

# 57. 监控与运行日志

Automation Center 应用日志：

```text
stdout
stderr
```

Docker 收集。

日志至少区分：

```text
INFO
WARNING
ERROR
```

关键字段建议携带：

```text
task_id
node_id
package_id
salt_jid
```

方便故障定位。

------

# 58. V1 推荐技术栈

该部分为推荐，不属于硬性需求。

后端可采用：

```text
Python
FastAPI
SQLAlchemy
SQLite
```

理由：

- 与 Salt/Python 生态接近；
- REST 开发简单；
- 支持异步 HTTP；
- 适合 SSE/WebSocket；
- 当前规模不需要复杂框架。

前端可采用：

```text
Vue 3
或 React
```

V1 无特殊约束。

------

# 59. V1 明确不引入

```text
Redis
RabbitMQ
Kafka
Celery Broker
PostgreSQL
MinIO
Kubernetes
Workflow Engine
```

如果未来：

```text
管理节点 > 数百
多 Automation Center 实例
要求 HA
复杂调度
```

再重新评估。

------

# 60. 核心时序

## Task 执行

```text
User
 │
 ▼
Web
 │
 ▼
Task API
 │
 ├─ Save Task
 │
 ▼
Scheduler
 │
 ├─ Acquire Node Lock
 │
 ▼
Execution Engine
 │
 ▼
Salt Adapter
 │
 ▼
salt-api
 │
 ▼
salt-master
 │
 ▼
salt-minion
 │
 ├─ Download Package
 │
 ├─ Execute Step
 │
 └─ Write Runtime Logs
 │
 ▼
Log Service
 │
 ▼
Web Real-time Log
```

------

# 61. 最终架构原则

V1 的核心不是构建一个大型自动化平台，而是建立一层稳定的业务控制面：

```text
Package
+
Node
+
Task
+
Step
+
Result
```

SaltStack 继续承担底层远程执行能力。

Automation Center 不重复实现：

```text
Agent
消息总线
远程认证
文件传输协议
远程命令执行协议
```

从而将开发工作集中到云平台维护任务本身。