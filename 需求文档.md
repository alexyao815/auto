# 需求文档

下面是基于前面全部需求访谈结果整理后的正式 V1 规格说明书。内容以**需求定义**为主，避免过早绑定具体后端框架、WebSocket/SSE 等实现细节。

# 云平台自动化维护中心 V1 需求规格说明书

**文档版本：** V1.0
**产品名称：** Automation Center / 云平台自动化维护中心
**适用阶段：** V1 产品需求与详细设计输入
**目标运行环境：** CentOS 9
**部署方式：** Docker 容器化部署

------

# 1. 文档目的

本文档用于定义云平台自动化维护中心 V1 的产品目标、功能范围、业务流程、功能需求、数据模型、维护包规范、任务执行规则、异常处理及非功能要求。

本系统主要面向云平台运维及研发人员，用于处理已经明确定位的：

- 产品 Bug；
- 产品缺陷；
- 不合理配置；
- 已知问题修复；
- 文件替换；
- 服务配置调整；
- 软件包更新；
- 系统级维护操作。

维护动作由研发侧独立的“维护包制作平台”完成开发、测试、校验和打包。

Automation Center 不负责维护脚本开发，仅负责：

1. 维护包上传与保存；
2. 节点管理；
3. 目标节点选择；
4. 任务下发；
5. Shell/Python 执行；
6. 执行状态跟踪；
7. 实时日志展示；
8. 执行结果保存；
9. 失败节点重新执行。

------

# 2. 产品定位

Automation Center 定位为：

> 面向云平台基础设施的轻量级自动化维护任务管理平台。

系统不是通用 CI/CD 平台，也不是完整配置管理平台，更不是持续配置收敛系统。

V1 重点解决以下问题：

```text
发现产品 Bug / 缺陷 / 配置问题
             │
             ▼
      研发制作维护包
             │
             ▼
    Automation Center
             │
       上传维护包
             │
       选择目标节点
             │
       创建维护任务
             │
             ▼
         salt-api
             │
             ▼
        salt-master
             │
             ▼
        salt-minion
             │
             ▼
      Shell / Python
             │
             ▼
       执行结果 / 日志
```

------

# 3. 用户范围

系统主要由：

- 云平台运维人员；
- 产品研发人员；

共同使用。

V1 不设计复杂用户及权限体系。

所有成功登录的用户：

- 查看相同页面；
- 拥有相同权限；
- 可以执行相同操作。

------

# 4. V1 产品范围

V1 包含以下核心模块：

```text
Automation Center
├── Dashboard
├── Node Center
├── Package Center
├── Task Center
├── System Settings
└── Authentication
```

底层执行能力由 SaltStack 提供：

```text
Automation Center
       │
       ▼
   salt-api
       │
       ▼
 salt-master
       │
       ▼
 salt-minion
       │
       ├── Shell
       └── Python
```

------

# 5. V1 明确不包含的功能

以下功能不属于 V1：

- 审批流程；
- 灰度发布；
- 分批发布；
- 定时任务；
- 周期任务；
- 任务回滚；
- 维护包历史版本文件保留；
- 复杂 RBAC；
- LDAP；
- AD；
- SSO；
- 多租户；
- 云平台组件配置中心；
- Nova/Neutron/Ceph/OVS 配置模型管理；
- 持续配置检查及自动收敛；
- 维护包在线编辑；
- Shell/Python 在线开发；
- Git 仓库同步；
- 维护包研发及制作；
- Automation Center HA；
- Salt Master HA；
- 数据库 HA；
- 正式外部开放 REST API；
- Ansible Playbook 执行；
- 任务优先级；
- 人工调整任务队列；
- Running Task 人工终止。

------

# 6. 总体架构

## 6.1 逻辑架构

```text
                  Automation Center
        ┌──────────────────────────────┐
        │ Dashboard                    │
        │ Node Center                  │
        │ Package Center               │
        │ Task Center                  │
        │ System Settings              │
        │ Authentication               │
        └──────────────┬───────────────┘
                       │
                       │ HTTP API
                       ▼
                  ┌──────────┐
                  │ salt-api │
                  └────┬─────┘
                       │
                       ▼
                ┌─────────────┐
                │ salt-master │
                └──────┬──────┘
                       │
             ──────────┼──────────
                       │
        ┌──────────────┼──────────────┐
        ▼              ▼              ▼
   salt-minion     salt-minion    salt-minion
    compute01       compute02       network01
        │              │              │
        ▼              ▼              ▼
      Shell          Python         Shell
```

## 6.2 Automation Center 内部架构

```text
Docker Host / CentOS 9
│
├── Automation Center Container
│   ├── Web
│   ├── Backend API
│   ├── Task Management
│   ├── Node Management
│   ├── Package Management
│   └── SQLite
│
└── Persistent Storage
    └── /var/lib/automation-center/
        ├── data/
        ├── packages/
        └── temp/
```

SQLite 数据文件及维护包文件必须持久化到宿主机，不能仅存在于容器可写层。

------

# 7. Dashboard

V1 首页提供轻量 Dashboard。

至少展示：

## 7.1 节点统计

```text
Total Nodes
Online Nodes
Offline Nodes
Disabled Nodes
```

## 7.2 维护包统计

```text
Package Count
```

## 7.3 任务统计

至少包括：

```text
Waiting
Running
Success
Partial Success
Failed
```

## 7.4 最近任务

展示最近执行任务，例如：

```text
Task Name          Package        Status          Created Time
--------------------------------------------------------------
xxx                nova-fix       Success         ...
xxx                ovs-fix        Running         ...
```

Dashboard 不承担复杂监控和图表分析。

------

# 8. Node Center

# 8.1 节点来源

所有受管节点安装：

```text
salt-minion
```

新 Minion 首次连接 Salt Master 后，以 Unaccepted Key 状态出现。

Automation Center 应提供：

```text
待接入节点
├── Accept
└── Reject
```

用户 Accept 后，该节点进入 Node Center。

不得默认开启无条件 Salt Key 自动接受机制。

------

# 8.2 节点基础信息

Node Center 至少维护：

```text
Node
├── hostname
├── management_ip
├── roles[]
├── online_status
├── enabled_status
└── last_check_time
```

一个节点允许拥有多个角色。

例如：

```text
node01
├── controller
├── network
└── ceph
```

------

# 8.3 支持角色

V1 核心角色包括：

```text
compute
network
controller
ceph
```

系统设计应允许后续增加其他角色。

------

# 8.4 角色自动发现

节点角色默认通过节点实际运行服务或进程自动识别。

示例：

```text
nova-compute             → compute
ceph-osd                 → ceph
ovn-controller / OVS     → network
nova-api 等控制服务       → controller
```

角色识别规则不建议硬编码在业务程序中，应作为 System Settings 中的规则进行配置。

------

# 8.5 角色人工修正

自动发现完成后，用户可以在 Web 中：

- 添加角色；
- 删除角色；
- 修改自动识别结果。

人工配置结果作为 Node Center 当前有效角色。

------

# 8.6 在线状态

节点状态包括：

```text
Online
Offline
```

Automation Center 应：

1. 后台定时通过 Salt `test.ping` 探测节点；
2. 保存最近一次状态；
3. 保存最近检查时间；
4. Node Center 页面提供“手工刷新”功能。

具体自动探测周期由系统配置决定。

------

# 8.7 Enabled / Disabled

节点除 Online/Offline 外，还应支持：

```text
Enabled
Disabled
```

规则：

```text
Online + Enabled
→ 可正常执行任务

Online + Disabled
→ 不允许选择执行

Offline
→ 可以选择，但创建任务前必须警告
```

Disabled 节点不得通过“强制执行”绕过。

------

# 8.8 节点删除

允许从 Node Center 删除节点。

节点删除后：

- 当前 Node 对象删除；
- 历史 Task 不删除；
- 历史 Task 保留执行时节点快照。

节点快照至少包含：

```text
hostname
management_ip
roles
```

因此历史任务展示不得依赖当前 Node 对象仍然存在。

------

# 8.9 Node 列表字段

默认展示：

```text
Hostname
Management IP
Roles
Online / Offline
Enabled / Disabled
Last Check Time
```

------

# 9. Package Center

# 9.1 产品边界

维护包由独立的研发出包平台生成。

Automation Center 不负责：

- 开发脚本；
- 修改脚本；
- 在线编辑；
- 编译；
- 构建；
- 完整业务规则校验。

Automation Center 信任出包平台生成的维护包。

------

# 9.2 外层维护包

用户上传的是一个外层压缩包。

外层包本身不做完整性校验。

外层包解压后至少包含：

```text
outer-package/
├── inner-package.tar.gz
└── inner-package.sha256
```

具体文件名可以由维护包规范确定。

------

# 9.3 SHA256 校验

上传流程：

```text
上传外层包
    │
    ▼
解压至临时目录
    │
    ├── SHA256 文件
    │
    └── 内层压缩包
            │
            ▼
     计算内层包 SHA256
            │
      ┌─────┴─────┐
      │           │
    一致         不一致
      │           │
      ▼           ▼
   继续处理      上传失败
```

Automation Center 仅对内层维护包执行 SHA256 完整性校验。

------

# 9.4 内层维护包

内层维护包为真正下发到节点执行的内容。

示例：

```text
inner-package.tar.gz
├── manifest.yaml
├── scripts/
│   ├── precheck.sh
│   ├── fix.py
│   └── verify.sh
└── files/
    ├── *.rpm
    ├── *.conf
    ├── *.py
    ├── binary
    ├── *.patch
    └── other resources
```

维护包可以携带任意普通修复资源。

Automation Center 不理解 `files/` 目录中的业务含义。

由 Shell/Python Step 自行完成：

- 文件备份；
- 文件替换；
- RPM 安装；
- patch 应用；
- 配置修改；
- 服务重启；
- 业务检查。

------

# 9.5 manifest.yaml

维护包必须携带 `manifest.yaml`，用于描述维护包元数据和执行流程。

建议模型：

```yaml
name: nova-compute-fix-001

description: 修复 nova-compute 已知问题

component: nova

bug_id: BUG-12345

target_roles:
  - compute

applicable_versions:
  - Yoga

steps:
  - name: precheck
    type: shell
    script: scripts/precheck.sh
    timeout: 300
    failure_action: stop

  - name: fix
    type: python
    script: scripts/fix.py
    timeout: 1800
    failure_action: stop

  - name: verify
    type: shell
    script: scripts/verify.sh
    failure_action: ignore
```

------

# 9.6 manifest 规则

## Step 顺序

按照：

```yaml
steps:
  - ...
  - ...
  - ...
```

数组自然顺序执行。不增加 `order` 字段。

## Step 名称

同一个维护包内：

```text
Step.name
```

必须唯一。

## Step 类型

V1 实现：

```text
shell
python
```

预留：

```text
ansible
```

但 V1 不提供 Ansible Executor。

若维护包存在 V1 不支持的执行类型，该维护包不得创建可执行任务，并应提示“不支持的 Executor 类型”。

## timeout

单位统一为：

```text
秒
```

例如：

```yaml
timeout: 300
```

表示 300 秒。

如果 Step 未设置 timeout：

```text
使用 System Settings 中的默认 Step Timeout
```

系统内置默认值：

```text
1800 秒
```

即 30 分钟。

## failure_action

字段名称统一为：

```text
failure_action
```

支持：

```text
stop
ignore
```

语义：

```text
stop
→ 当前 Step 失败后，不执行当前节点后续 Step

ignore
→ 当前 Step 记录失败，但继续执行后续 Step
```

未指定时：

```text
failure_action = stop
```

------

# 9.7 Shell 与 Python 混合执行

同一维护包允许混合使用：

```text
Shell
Python
```

例如：

```text
Step1 Shell
Step2 Python
Step3 Shell
```

------

# 9.8 维护包参数

V1 不支持任务执行时动态传参。

维护包内容和执行参数均由出包平台提前固定。

Automation Center 不提供：

```text
--timeout
--service-name
--config-value
```

等运行时业务参数输入页面。

------

# 9.9 Package 元数据导入

SHA256 校验成功后，Automation Center 临时解压内层包并读取 `manifest.yaml`，将以下信息保存到 SQLite：

```text
name
description
component
bug_id / defect_id
target_roles[]
applicable_versions[]
steps[]
```

Automation Center 不承担复杂 manifest 业务合法性检查。

但如果 manifest 无法读取或无法获得执行所需元数据，则该包无法正常入库。

------

# 9.10 Package 存储

完成处理后：

- 长期保存内层压缩包；
- 不长期保存解压目录；
- 删除上传和解析过程中的临时目录。

示例：

```text
/var/lib/automation-center/packages/
└── <package-id>/
    └── package.tar.gz
```

------

# 9.11 Package 上传大小

最大上传大小可通过 System Settings 配置。

默认：

```text
10 GB
```

------

# 9.12 Package Revision

V1 不建设完整版本管理系统，但提供轻量 Revision。

首次上传：

```text
v1
```

第一次 Update：

```text
v2
```

第二次 Update：

```text
v3
```

依次递增。

Revision 仅表示该逻辑维护包已经被更新多少次。

不保存：

```text
v1.tar.gz
v2.tar.gz
v3.tar.gz
```

等历史实体文件。

系统只保存当前 Revision 对应的维护包文件。

------

# 9.13 同名包处理

普通 Upload 操作：

```text
如果 Package Name 已存在
→ 拒绝上传
→ 提示用户使用“更新维护包”
```

不得通过普通上传覆盖现有维护包。

------

# 9.14 更新维护包

Package Center 提供独立：

```text
更新维护包
```

功能。

更新后：

```text
当前文件被新文件覆盖
revision + 1
```

例如：

```text
v2 → v3
```

若该 Package 当前存在：

```text
Running Task
Waiting Task
```

则禁止更新。

------

# 9.15 删除维护包

允许删除维护包。

删除后：

- Package 当前文件删除；
- Package 当前业务对象删除；
- 历史 Task 保留；
- 历史执行结果保留。

如果 Package 当前存在：

```text
Running Task
Waiting Task
```

则禁止删除。

------

# 9.16 历史任务与包更新

例如：

```text
Task-001 使用 Package v2
```

之后 Package 更新为：

```text
v3
```

因为 v2 实体文件已经不存在，因此：

```text
Task-001 不允许直接 Retry
```

系统提示：

> 原维护包 Revision 已不存在，请基于当前 Revision 创建新任务。

------

# 9.17 历史任务与包删除

如果历史 Task 对应 Package 已删除：

```text
不允许 Retry
```

但历史任务页面仍然展示：

```text
Package Name
Package Revision
```

等快照信息。

------

# 9.18 Package 角色信息

维护包通过：

```yaml
target_roles:
```

声明建议目标角色。

该字段：

```text
用于提示
不作为强制安全限制
```

例如：

```text
Package target role = compute
用户选择 network01
```

系统必须：

1. 显示角色不匹配警告；
2. 弹出二次确认；
3. 用户明确确认后仍允许执行。

------

# 9.19 Applicable Versions

Package 可以携带：

```yaml
applicable_versions:
```

用于说明维护包适用产品或组件版本。

Automation Center：

- 只负责展示该信息；
- 不自动采集节点 Nova/Neutron/OVS/Ceph 版本；
- 不自动判断兼容性；
- 不因版本信息强制禁止任务执行。

版本判断由使用人员负责。

------

# 9.20 Package 列表字段

默认展示：

```text
Package Name
Revision
Component
Bug / Defect ID
Target Roles
Applicable Versions
Upload Time
Description
```

------

# 10. Task Center

# 10.1 任务创建方式

V1 仅支持：

```text
立即执行
```

不支持：

- 定时执行；
- Cron；
- 周期执行；
- 延迟执行。

------

# 10.2 目标节点选择

用户可以通过两种方式选择目标：

## 按角色

```text
compute
network
controller
ceph
```

## 直接选择节点

例如：

```text
compute01
compute02
network01
```

两种方式均需要支持。

------

# 10.3 Disabled 节点

Disabled 节点：

```text
不可被选中执行
```

------

# 10.4 Offline 节点

Offline 节点：

```text
允许进入任务
```

但在任务确认页必须显示明显警告。

任务开始后：

```text
Offline Node
→ Node Task = Failed
→ failure_reason = Offline
```

后续满足重试条件时允许 Retry。

------

# 10.5 创建任务确认页

创建任务必须经过确认页。

至少展示：

```text
Package Name
Package Revision
Description
Applicable Versions
Target Roles
实际选择节点
Online / Offline 状态
角色不匹配警告
用户 Remark
```

用户确认后才能执行。

------

# 10.6 Task 名称

Task Name 由系统自动生成。

例如：

```text
fix-nova-001-v3-20260811-001
```

用户不需要手工填写 Task Name。

------

# 10.7 Remark

创建 Task 时提供：

```text
remark
```

用户可以填写，也可以留空。

例如：

```text
修复 BUG-12345，处理 compute 节点问题
```

------

# 10.8 防重复创建

点击“立即执行”后：

前端：

```text
立即禁用执行按钮
```

后端：

```text
必须提供幂等保护
```

避免浏览器重复点击或重复请求创建多个相同 Task。

------

# 10.9 多节点执行

当前环境最大约 30 台服务器。

V1 不设计：

- 灰度；
- 批次；
- 用户配置并发数。

目标节点原则上：

```text
全部并发执行
```

------

# 10.10 节点执行锁

同一个节点：

```text
同一时刻只能运行一个 Task
```

如果节点已经有 Task 正在运行：

```text
新任务进入该节点 Waiting 队列
```

------

# 10.11 节点级队列

队列粒度：

```text
Node
```

而不是整个 Task。

例如：

```text
Task-A:

compute01 Idle
→ 立即运行

compute02 Busy
→ Waiting

compute03 Idle
→ 立即运行
```

Task 不因为其中一个节点 Busy 而整体等待。

------

# 10.12 队列顺序

节点队列采用：

```text
FIFO
```

即：

```text
先创建
先执行
```

V1 不支持：

- Task Priority；
- 调整队列；
- 插队。

------

# 10.13 Waiting Task 取消

如果任务尚未真正运行，仅处于：

```text
Waiting
```

允许用户取消。

Running Task：

```text
不允许停止
```

------

# 10.14 Task Step 执行

每个 Node 在自己的任务上下文中按 `steps[]` 顺序依次执行。

例如：

```text
Node compute01

Step1
  ↓
Step2
  ↓
Step3
```

不同节点之间并发。

同一个节点上的 Step 串行。

------

# 10.15 Step 执行用户

所有 Shell/Python Step：

```text
统一以 root 权限执行
```

V1 不设计：

- run_as；
- sudo 用户；
- Step 自定义用户；
- 用户权限切换模型。

------

# 10.16 Step Timeout

Step 可定义：

```yaml
timeout: <seconds>
```

超时后：

```text
Step = Failed
failure_reason = Timeout
```

然后根据：

```text
failure_action
```

决定：

```text
stop
或
ignore
```

------

# 10.17 failure_action

## stop

```text
Step Failed
   ↓
当前节点后续 Step 不再执行
   ↓
后续 Step = Skipped
```

## ignore

```text
Step Failed
   ↓
记录失败
   ↓
继续执行下一 Step
```

------

# 10.18 工作目录

每个 Node 上的 Task 使用独立工作目录。

统一规范：

```text
/var/lib/automation-center/tasks/<task_id>/
├── package.tar.gz
└── work/
    ├── manifest.yaml
    ├── scripts/
    └── files/
```

脚本应以维护包 `work/` 根目录作为执行上下文，不应依赖随机临时路径。

------

# 10.19 工作目录清理

Task 在该节点：

## 成功

```text
自动删除工作目录
```

## 失败

```text
保留工作目录
```

用于现场排查。

失败任务工作目录：

```text
固定保留 7 天
```

之后自动清理。

------

# 10.20 失败节点 Retry

任务执行失败后，可以对：

```text
失败节点
```

单独执行 Retry。

Retry：

```text
必须从 Step1 开始重新执行整个维护包
```

不能从失败 Step 接着执行。

原因：

> 维护包内部可能包含环境检查、状态检查和前置准备动作。

------

# 10.21 Retry Attempt 模型

Retry 不创建完全独立的新 Task。

在原 Task 下记录 Attempt：

```text
Task-001
└── compute01
    ├── Attempt 1
    │   └── Failed
    │
    └── Attempt 2
        └── Success
```

每个 Attempt 都应独立记录 Step 执行结果。

------

# 10.22 Retry 条件

只有满足以下条件时才能 Retry：

```text
Package 仍然存在
且
Package Revision 与原 Task 一致
```

如果：

```text
Package 已删除
```

或：

```text
Package v2 已更新为 v3
```

则禁止 Retry。

------

# 10.23 复制任务

历史 Task 页面支持：

```text
复制任务
```

复制时自动带出：

- Package；
- 当前可用 Revision；
- 原目标节点；
- Remark。

用户进入任务创建流程后重新确认再执行。

复制任务本质上创建新 Task。

------

# 11. Task 状态模型

# 11.1 Task 总体状态

至少支持：

```text
Waiting
Running
Success
Partial Success
Failed
```

如果增加 Waiting 取消操作，实现层可增加：

```text
Cancelled
```

用于明确记录取消结果。

------

# 11.2 Task 状态判定

## Waiting

任务已经创建，但所有目标 Node 尚未开始执行。

## Running

只要存在目标节点：

```text
Running
```

或者由于节点执行锁处于等待队列中，则 Task 处于活动状态。

## Success

所有目标节点最终成功。

## Partial Success

部分节点成功，同时存在：

```text
Failed
Offline
```

等失败节点。

例如：

```text
8 Success
1 Failed
1 Offline

→ Partial Success
```

## Failed

所有目标节点最终均失败。

------

# 11.3 Node Task 状态

建议至少支持：

```text
Waiting
Running
Success
Failed
Offline
Cancelled
```

其中 Offline 属于明确失败原因，也可以在实现层采用：

```text
status = Failed
failure_reason = Offline
```

统一处理。

------

# 11.4 Step 状态

建议至少支持：

```text
Waiting
Running
Success
Failed
Skipped
```

------

# 12. 执行结果

系统必须记录以下 6 类核心信息：

1. Task 总体状态；
2. 每个节点状态；
3. 每个 Step 状态；
4. stdout；
5. stderr；
6. exit_code。

------

# 13. Task 页面展示

## 13.1 默认 Task Detail

默认主要展示：

```text
Task Overall Status

Node
├── compute01     Success
├── compute02     Running
├── compute03     Failed
└── compute04     Waiting
```

默认页面不一次性展开全部 stdout/stderr。

------

# 13.2 节点详情

用户点击一个节点后查看：

```text
Node
├── Attempt
│   ├── Step1
│   │   ├── Status
│   │   ├── stdout
│   │   ├── stderr
│   │   └── exit_code
│   │
│   └── Step2
│       └── ...
```

------

# 13.3 实时日志

任务执行期间必须支持：

```text
stdout
stderr
```

实时滚动展示。

用户无需等待 Step 完成后才能查看日志。

V1 一次只实时查看：

```text
一个节点
```

不要求 30 台节点 stdout 同时滚动。

具体实时通信技术：

- WebSocket；
- SSE；
- 其他流式方式；

不在需求层强制限定，由详细设计决定。

------

# 13.4 多浏览器查看

允许多个浏览器同时：

- 查看同一个 Task；
- 查看任务状态；
- 查看实时日志。

查看行为不能锁定 Task。

------

# 13.5 浏览器关闭

Task 生命周期必须由服务器端管理。

用户：

```text
关闭浏览器
退出登录
网络断开
```

均不得导致正在运行的 Task 停止。

------

# 13.6 Task 列表

默认展示：

```text
Task ID
Task Name
Package
Revision
Status
Target Node Count
Success Count
Failed Count
Created Time
Remark
```

------

# 14. 日志保留

Task、NodeTask、Step、Attempt 等结构化元数据：

```text
长期保留
```

stdout/stderr：

```text
按 retention 参数自动清理
```

System Settings 中提供：

```text
execution_log_retention_days
```

如果用户未配置：

```text
默认 7 天
```

日志到期删除后，Task 状态及结构化执行结果仍应保留。

------

# 15. System Settings

原“Config Center”正式定义为：

> System Settings / 系统配置

其职责仅为管理 Automation Center 自身配置。

不管理云平台业务组件配置。

------

# 15.1 Salt Settings

包括：

```text
salt_api_url
salt_api_username
salt_api_credential
request_timeout
```

敏感信息在 Web 页面中不得明文回显。

------

# 15.2 Package Settings

包括：

```text
package_storage_path
temp_path
max_upload_size
```

默认：

```text
max_upload_size = 10GB
```

------

# 15.3 Execution Settings

至少包括：

```text
default_step_timeout
execution_log_retention_days
```

默认：

```text
default_step_timeout = 1800 秒
execution_log_retention_days = 7 天
```

------

# 15.4 Node Settings

包括：

```text
node_status_check_interval
role_detection_rules
```

节点探测周期可配置。

角色识别规则可维护。

------

# 16. Authentication

# 16.1 用户来源

Automation Center 使用自身独立账号。

该账号：

- 不是 Linux 系统账号；
- 不是 Salt PAM 账号；
- 与 Salt API 用户体系独立。

------

# 16.2 V1 账号模型

V1 使用固定账号和密码。

不建设：

- 用户注册；
- 用户管理；
- 密码分级权限；
- RBAC；
- LDAP；
- SSO。

------

# 16.3 密码要求

固定账号密码不得明文硬编码在前端。

服务端应采用不可逆密码 Hash 保存登录密码。

------

# 16.4 权限

所有登录用户权限一致。

------

# 17. 操作审计

由于 V1 采用固定共享账号，不要求识别具体研发或运维人员。

系统至少记录：

```text
login_account
source_ip
operation_time
operation_type
related_object
```

关联对象包括：

```text
package_id
task_id
node_id
```

典型审计操作：

- 登录；
- 接受 Minion；
- 拒绝 Minion；
- 修改 Node Role；
- Enable/Disable Node；
- 删除 Node；
- 上传 Package；
- 更新 Package；
- 删除 Package；
- 创建 Task；
- Retry；
- 取消 Waiting Task；
- 修改 System Settings。

------

# 18. Salt 不可用处理

如果创建任务时：

```text
salt-api
或
salt-master
```

不可用：

```text
任务创建失败
```

Web 必须明确提示：

> Salt 执行服务不可用。

V1 不设计：

```text
Salt 恢复后自动执行之前失败的创建请求
```

------

# 19. Automation Center 重启恢复

如果 Automation Center 重启时存在：

```text
Running Task
```

服务启动后必须：

1. 查询 Salt Job 状态；
2. 重新关联已有执行任务；
3. 恢复 Task / NodeTask 状态；
4. 避免将正在 Salt 侧执行的任务重复下发。

不能简单把所有未完成 Task 标记为失败。

------

# 20. 任务异常

执行过程中如果出现：

- Salt 调用失败；
- Minion 失联；
- Step Timeout；
- 脚本 exit_code 非零；
- 文件下发失败；

系统需要：

1. 保存错误原因；
2. 更新对应 Step/Node 状态；
3. 保存已获取的 stdout/stderr；
4. 根据 `failure_action` 决定后续 Step；
5. 任务结束后按总体状态规则汇总。

------

# 21. 数据模型

V1 建议包含以下主要实体。

## 21.1 Node

```text
Node
├── id
├── salt_minion_id
├── hostname
├── management_ip
├── online_status
├── enabled_status
├── last_check_time
├── created_at
└── updated_at
```

------

## 21.2 Role

```text
Role
├── id
└── name
```

Node 与 Role：

```text
多对多
```

------

## 21.3 Package

```text
Package
├── id
├── name
├── revision
├── description
├── component
├── bug_id
├── target_roles
├── applicable_versions
├── file_path
├── sha256
├── upload_time
└── metadata
```

------

## 21.4 PackageStep

```text
PackageStep
├── id
├── package_id
├── sequence
├── name
├── type
├── script
├── timeout
└── failure_action
```

`sequence` 可以由 `steps[]` 数组位置生成，不要求 manifest 显式提供。

------

## 21.5 Task

```text
Task
├── id
├── name
├── package_name_snapshot
├── package_revision_snapshot
├── status
├── target_node_count
├── success_count
├── failed_count
├── remark
├── created_at
├── started_at
└── finished_at
```

------

## 21.6 TaskNode

```text
TaskNode
├── id
├── task_id
├── node_id
├── hostname_snapshot
├── management_ip_snapshot
├── roles_snapshot
├── status
├── failure_reason
├── queue_time
├── started_at
└── finished_at
```

------

## 21.7 TaskAttempt

```text
TaskAttempt
├── id
├── task_node_id
├── attempt_no
├── status
├── started_at
└── finished_at
```

------

## 21.8 TaskStepResult

```text
TaskStepResult
├── id
├── attempt_id
├── step_name
├── sequence
├── executor_type
├── status
├── exit_code
├── stdout
├── stderr
├── failure_reason
├── started_at
└── finished_at
```

stdout/stderr 的具体物理存储方式由详细设计确定，不要求全部作为 SQLite 大字段存储。

------

## 21.9 SystemSetting

```text
SystemSetting
├── key
├── value
├── sensitive
└── updated_at
```

------

## 21.10 AuditLog

```text
AuditLog
├── id
├── account
├── source_ip
├── operation
├── object_type
├── object_id
├── detail
└── created_at
```

------

# 22. SQLite

V1 使用：

```text
SQLite
```

作为 Automation Center 数据库。

选择依据：

- 单节点部署；
- 被管节点规模约 30 台；
- 不要求 HA；
- 简化部署；
- 降低额外基础设施依赖。

SQLite 文件必须持久化。

例如：

```text
/var/lib/automation-center/data/automation.db
```

如果未来演进为：

- 多实例 Web/API；
- HA；
- 大规模并发；

再考虑迁移 PostgreSQL。

------

# 23. 文件存储

V1 使用管理节点本地文件系统。

不引入：

- MinIO；
- S3；
- NFS。

建议：

```text
/var/lib/automation-center/
├── data/
│   └── automation.db
├── packages/
└── temp/
```

------

# 24. API 边界

Automation Center Web 前端使用后端 REST API。

V1：

```text
API 仅服务 Automation Center 自身 Web
```

不作为正式外部开放 API。

因此 V1 不要求：

- API Key 管理；
- 第三方调用授权；
- OpenAPI 对外兼容承诺；
- 外部 API SLA。

但内部 API 应保持清晰的资源模型，方便未来扩展。

------

# 25. 主要页面

V1 至少包含：

```text
1. Login
2. Dashboard
3. Node Center
4. Pending Nodes
5. Package Center
6. Package Detail
7. Package Upload
8. Package Update
9. Task Center
10. Create Task
11. Task Confirmation
12. Task Detail
13. Node Execution Detail
14. System Settings
```

不单独建设 Result Center。

执行结果属于：

```text
Task Center
```

------

# 26. 典型业务流程

## 26.1 新节点接入

```text
安装 salt-minion
      ↓
连接 salt-master
      ↓
Unaccepted Key
      ↓
Automation Center
Pending Nodes
      ↓
Accept
      ↓
Node Center
      ↓
探测服务
      ↓
自动识别 Role
      ↓
人工可修正
```

------

# 26.2 维护包上传

```text
研发出包平台
      ↓
生成外层维护包
      ↓
Web 上传
      ↓
解压外层包
      ↓
读取 SHA256 + 内层包
      ↓
校验 SHA256
      ↓
临时读取 manifest
      ↓
保存 Package Metadata
      ↓
保存内层压缩包
      ↓
清理临时目录
```

------

# 26.3 创建维护任务

```text
选择 Package
      ↓
查看适用版本/角色
      ↓
按 Role 或 Node 选择目标
      ↓
检查 Disabled
      ↓
提示 Offline
      ↓
提示 Role mismatch
      ↓
填写可选 Remark
      ↓
确认页
      ↓
立即执行
```

------

# 26.4 任务执行

```text
Task Created
      ↓
检查每个 Node
      │
      ├── Idle
      │     ↓
      │   Running
      │
      └── Busy
            ↓
          Waiting
            ↓
          FIFO
            ↓
          Running
```

节点开始后：

```text
下载维护包
    ↓
创建工作目录
    ↓
解压
    ↓
Step1
    ↓
Step2
    ↓
...
    ↓
生成结果
```

------

# 26.5 Failure Action

```text
Step Failed
    │
    ├── failure_action = stop
    │       ↓
    │   后续 Step Skipped
    │
    └── failure_action = ignore
            ↓
        执行下一 Step
```

------

# 26.6 Retry

```text
Node Failed
    ↓
用户点击 Retry
    ↓
检查 Package
    │
    ├── Package 不存在
    │       → 禁止
    │
    ├── Revision 已变化
    │       → 禁止
    │
    └── Package/Revision 一致
            ↓
        新增 Attempt
            ↓
        从 Step1 重跑
```

------

# 27. 非功能要求

## 27.1 规模

V1 目标被管节点规模：

```text
≤ 30 台左右
```

无需针对万级节点进行架构设计。

------

## 27.2 高可用

V1 不要求 HA。

允许：

```text
Automation Center 单点
SQLite 单点
salt-master 单点
salt-api 单点
```

------

## 27.3 部署

V1 Automation Center 使用：

```text
Docker
```

进行容器化部署。

宿主机目标环境：

```text
CentOS 9
```

------

## 27.4 应用日志

Web/API 服务日志输出至：

```text
stdout / stderr
```

由 Docker / 宿主机日志体系管理。

应用自身不建设复杂日志轮转模块。

------

## 27.5 安全

V1 至少满足：

- 登录密码不得明文存储；
- Salt API credential 不得在 Web 明文展示；
- 上传包只允许在受控目录解压；
- 解压逻辑必须避免路径穿越到应用工作目录以外；
- Task 脚本只在指定工作目录执行；
- Disabled 节点不得执行任务；
- Running Task 不得被修改其对应 Package。

------

# 28. 关键约束

## 28.1 Automation Center 不理解业务修复逻辑

Automation Center 不需要知道：

```text
Nova 是什么
Ceph 参数是什么意思
OVS 参数是否合法
某 RPM 是否应该安装
某服务是否应该 restart
```

这些业务逻辑均属于维护包。

Automation Center 只理解：

```text
Package
Node
Task
Step
Executor
Result
```

------

# 28.2 配置修改统一通过维护包

例如：

```text
修改 nova.conf
设置 OVS other_config
修改 Ceph 参数
修改系统 sysctl
```

均通过：

```text
Shell / Python Package
```

执行。

不建设额外“云平台组件配置中心”。

------

# 28.3 管理面应保持轻量

V1 不引入：

```text
PostgreSQL
MySQL
Redis
RabbitMQ
Kafka
MinIO
NFS
复杂 Workflow Engine
```

除非后续实际需求证明有必要。

------

# 29. V1 验收核心场景

V1 至少完成以下端到端场景。

## 场景 1：节点接入

```text
Minion 注册
→ Web 待接入
→ Accept
→ Node Center 展示
→ 自动识别 Role
→ test.ping 在线
```

## 场景 2：Shell 单 Step

```text
上传 Package
→ SHA256 成功
→ 选择 Node
→ 执行 shell
→ 实时查看日志
→ Success
```

## 场景 3：Shell + Python 多 Step

```text
Step1 Shell
Step2 Python
Step3 Shell
```

按顺序执行并正确保存结果。

## 场景 4：failure_action=stop

```text
Step2 Failed
→ Step3 Skipped
```

## 场景 5：failure_action=ignore

```text
Step2 Failed
→ Step3 继续执行
```

## 场景 6：Offline 节点

```text
10 个节点
1 Offline

→ 9 个正常执行
→ Offline Node Failed
→ Task Partial Success
```

## 场景 7：节点排队

```text
Task-A 正在 compute01 执行
Task-B 同时选择 compute01

→ Task-B compute01 Waiting
→ Task-A 完成
→ Task-B 自动开始
```

## 场景 8：部分节点 Busy

```text
compute01 Idle
compute02 Busy
compute03 Idle

→ 01/03 立即执行
→ 02 Waiting
```

## 场景 9：Retry

```text
compute02 Failed
→ Retry
→ Attempt 2
→ 从 Step1 开始
```

## 场景 10：Package 已升级

```text
原 Task = v2
当前 Package = v3
→ Retry 禁止
```

## 场景 11：实时日志

长时间运行 Step：

```text
stdout/stderr
```

能够在执行期间持续显示。

## 场景 12：Automation Center 重启

```text
Task Running
→ Automation Center Restart
→ 重新查询 Salt Job
→ 恢复原 Task 状态
→ 不重复执行
```

------

# 30. 后续详细设计重点

需求确认后，详细设计阶段重点需要解决：

1. SQLite Schema；
2. REST API 定义；
3. Salt API 调用封装；
4. Salt Job 与内部 Task/Attempt 的关联；
5. 实时 stdout/stderr 技术方案；
6. Node 级 FIFO 队列；
7. Node Execution Lock；
8. Docker Volume 设计；
9. Package 解压安全；
10. Manifest Parser；
11. Role Detection Engine；
12. Task 状态机；
13. 服务重启后的任务恢复；
14. 日志存储及 7 天清理机制；
15. 10GB 大文件上传及临时空间控制。

------

# 31. 设计原则总结

V1 遵循以下原则：

> **维护包负责“怎么修”，Automation Center 负责“在哪里执行、什么时候执行以及结果是什么”。**

核心边界：

```text
研发出包平台
负责：
脚本开发
业务校验
包构建
SHA256生成

          ↓

Automation Center
负责：
包管理
节点管理
任务管理
下发
执行状态
实时日志
失败重试

          ↓

SaltStack
负责：
Agent通信
文件下发
远程执行
Job管理

          ↓

目标节点
负责：
实际执行 Shell / Python
```

通过上述边界，使 V1 保持足够轻量，同时具备对云平台已知 Bug、缺陷和配置问题进行集中自动化修复的能力。

这份版本已经可以作为**产品需求评审和详细设计的输入文档**。下一阶段最适合继续拆成两份技术文档：一份《系统详细架构设计》，另一份《维护包 manifest.yaml 与出包规范》。