# TLS 证书目录

生产部署前放入：

- `tls.crt`：服务器证书及必要的中间证书链；
- `tls.key`：对应私钥，权限建议为 `0600`。

运行时证书和私钥均已被 `.gitignore` 排除，不得提交到仓库。目标环境生成
证书的完整步骤见 [`docs/deployment-192.168.200.11.md`](../docs/deployment-192.168.200.11.md)。
